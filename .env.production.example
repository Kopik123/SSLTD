# Production Environment Configuration for DigitalOcean
# Copy this file to .env and update with your actual values

# Application Environment
APP_ENV=production
APP_DEBUG=0
APP_URL=https://yourdomain.com
APP_KEY=CHANGE-THIS-TO-RANDOM-32-CHARACTERS

# Database Configuration - MySQL/MariaDB
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ss_ltd
DB_USER=ss_ltd_user
DB_PASS=CHANGE-THIS-TO-SECURE-PASSWORD

# Business Configuration
SERVICE_AREA_RADIUS_MILES=60

# Session Configuration
SESSION_NAME=ss_ltd_session
SESSION_LIFETIME=7200

# File Upload Limits (10MB default)
MAX_UPLOAD_SIZE=10485760

# IMPORTANT SECURITY NOTES:
# 1. Never commit the actual .env file to git
# 2. Use strong passwords for DB_PASS
# 3. Generate a random APP_KEY (at least 32 characters)
# 4. Set APP_DEBUG=0 in production
# 5. Use HTTPS (APP_URL should start with https://)
# 6. Restrict database access to localhost only
