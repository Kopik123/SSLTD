# Production Environment Configuration
# IMPORTANT: Never commit actual .env files!

APP_ENV=production
APP_DEBUG=0
APP_URL=https://your-production-domain.com
APP_KEY=MUST-BE-CHANGED-TO-SECURE-RANDOM-STRING

# Production Database (use external managed database service)
DB_CONNECTION=mysql
DB_HOST=your-db-host.com
DB_PORT=3306
DB_NAME=ss_ltd_prod
DB_USER=prod_user
DB_PASS=secure-production-password

# SSL/TLS for database (if required by provider)
# DB_SSL_CA=/path/to/ca-cert.pem
# DB_SSL_VERIFY=true

SERVICE_AREA_RADIUS_MILES=60

# Dev tools MUST be disabled in production
SS_DEV_TOOLS_KEY=

# Optional: Session configuration for serverless
# SESSION_DRIVER=database  # Use database instead of file-based sessions

# Optional: Upload configuration
# UPLOAD_MAX_SIZE=10485760  # 10MB in bytes
# UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,application/pdf
