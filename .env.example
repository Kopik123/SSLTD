APP_ENV=dev
APP_DEBUG=1
APP_URL=http://127.0.0.1:8000
APP_KEY=change-me

# Production notes:
# - Set APP_ENV=prod (or production) and APP_DEBUG=0.
# - Set APP_KEY to a strong random value (32+ chars). In prod the app will refuse
#   to start if APP_KEY is missing/weak.

# mysql|sqlite
DB_CONNECTION=mysql

# Used only when DB_CONNECTION=sqlite
DB_DATABASE=storage/app.db
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=ss_ltd
DB_USER=root
DB_PASS=

SERVICE_AREA_RADIUS_MILES=60
