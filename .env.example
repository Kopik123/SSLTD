# Application Environment
APP_ENV=dev
APP_DEBUG=1
APP_URL=http://127.0.0.1:8000
APP_KEY=change-me-to-random-string

# Database Configuration
# Use 'mysql' for production or 'sqlite' for local dev testing
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=ss_ltd
DB_USER=root
DB_PASS=

# SQLite path (used only when DB_CONNECTION=sqlite)
DB_DATABASE=storage/app.db

# Service Configuration
SERVICE_AREA_RADIUS_MILES=60
