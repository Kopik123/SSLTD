# Application Environment
# Set to 'dev' for development, 'staging' for staging, 'production' for production
APP_ENV=dev

# Enable debug mode (1=enabled, 0=disabled)
# WARNING: Always set to 0 in production!
APP_DEBUG=1

# Application URL (used for generating links)
# For XAMPP Apache: http://localhost/ss_ltd
# For PHP built-in server: http://127.0.0.1:8000
APP_URL=http://localhost/ss_ltd

# Application Secret Key (used for encryption/hashing)
# IMPORTANT: Generate a random string for production!
APP_KEY=change-me-to-random-string

# Database Connection Type
# Options: mysql, sqlite
# For XAMPP: use 'mysql'
DB_CONNECTION=mysql

# SQLite Database File (only used if DB_CONNECTION=sqlite)
# Leave as default for MySQL setup
DB_DATABASE=storage/app.db

# MySQL Database Configuration (XAMPP defaults)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=ss_ltd
DB_USER=root
DB_PASS=

# Service Area Configuration
# Default radius in miles for service area calculations
SERVICE_AREA_RADIUS_MILES=60

# Development Tools (optional)
# Uncomment to enable dev tools from non-loopback IPs
# SS_DEV_TOOLS_KEY=your-secret-key-here
